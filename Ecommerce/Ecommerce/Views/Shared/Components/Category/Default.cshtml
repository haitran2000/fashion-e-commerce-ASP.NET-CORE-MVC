@model IList<Ecommerce.Areas.Admin.Models.CategoryModel>
<div class="sidebar_widget catrgorie mb-35">
    <h3>Categories</h3>
    <ul>
        @foreach (var m in Model)
        {
            <li class="has-sub">
                <a href="#"><i class="fa fa-caret-right"></i>@m.Name.</a>
            </li>
        }
    </ul>
</div>