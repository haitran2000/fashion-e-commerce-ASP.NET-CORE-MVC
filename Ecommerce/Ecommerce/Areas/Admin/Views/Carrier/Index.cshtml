@model IEnumerable<Ecommerce.Areas.Admin.Models.CarriersModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CarrierName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CarrierStatus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CarrierName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CarrierStatus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CarrierID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CarrierID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CarrierID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
